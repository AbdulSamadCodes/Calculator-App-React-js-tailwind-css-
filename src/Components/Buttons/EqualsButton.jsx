import { React } from 'react';

function EqualsButton({btnText}) {
  return (
    <button className="button equals-button font-extrabold">
        {btnText}
    </button>
  );
}

export default EqualsButton;